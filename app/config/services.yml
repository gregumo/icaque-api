# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:

    faker.provider.foo:
        class: AppBundle\DataFixtures\Faker\Provider\FooProvider
        tags: [ { name: hautelook_alice.faker.provider } ]
# Examples of API resource mapping, learn more at
# https://github.com/dunglas/DunglasApiBundle

    resource.person:
        parent:    "api.resource"
        arguments: [ "AppBundle\\Entity\\Person" ]
        tags:      [ { name: "api.resource" } ]

    resource.fruit:
        parent:    "api.resource"
        arguments: [ "AppBundle\\Entity\\Fruit" ]
        tags:      [ { name: "api.resource" } ]

    resource.vegetable:
        parent:    "api.resource"
        arguments: [ "AppBundle\\Entity\\Vegetable" ]
        tags:      [ { name: "api.resource" } ]

    resource.organization.filter:
        parent:    "api.doctrine.orm.search_filter"
        arguments: [ { "name": "exact", "url": "partial" } ]

# A typical service
#    service_name:
#        class: AppBundle\Directory\ClassName
#        arguments: ["@another_service_name", "plain_value", "%parameter_name%"]
